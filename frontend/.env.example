# Frontend environment variables (copy to frontend/.env.local for local development)

# Backend API URL — no trailing slash
# Local dev with Vite proxy: leave empty or omit this file entirely
# Local dev without proxy: http://localhost:8000
# Production (Vercel): https://your-app.onrender.com
VITE_API_URL=
